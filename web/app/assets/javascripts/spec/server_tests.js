describe("reviews interactions", function () {

  it("should work", function () {
    var resources = TESTDATA.resources;
    var args = TESTDATA.args;
    codeAssignment($("#review-editor"), resources, args);
  });
});
